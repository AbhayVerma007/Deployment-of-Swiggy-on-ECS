version: 0.2

env:
  parameter-store:
    DOCKER_REGISTRY_USERNAME: /cicd/docker-credentials/username
    DOCKER_REGISTRY_PASSWORD: /cicd/docker-credentials/password
    DOCKER_REGISTRY_URL: /cicd/docker-registry/url
    SONAR_TOKEN: /cicd/sonar/sonar-token

phases:
  install:
    runtime-versions:
      python: 3.11
      java: corretto17
      
  pre_build:
    commands:
      - echo "Installing dependencies..."
      - echo "Ready to build..."

  build:
    commands:
      # Navigate into the subfolder before doing anything else!
      - cd swiggyclone
      - echo "Running tests..."
      - echo "Building Docker image..."
      - 'echo "$DOCKER_REGISTRY_PASSWORD" | docker login -u "$DOCKER_REGISTRY_USERNAME" --password-stdin "$DOCKER_REGISTRY_URL"'
      - 'docker build -t "$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/swiggy:latest" .'
      - 'docker push "$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/swiggy:latest"'

  post_build:
    commands:
      - 'echo "Build completed on `date`"'
      - echo "Sending email notification via SES..."
      - 'aws ses send-email --from "vermaabhay085@gmail.com" --to "vermaabhay085@gmail.com" --subject "CodeBuild Status: $CODEBUILD_BUILD_ID" --text "Build status: $CODEBUILD_BUILD_STATUS" --region "ap-south-1"'

artifacts:
  # Tell CodeBuild to look inside the subfolder for the output files
  base-directory: swiggyclone
  files:
    - appspec.yaml